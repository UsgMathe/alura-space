```
generated_by: n8n-workflow
workflow_name: GitHub Repository Context Builder
workflow_id: 8HscJrWZ64ZbBkVr
generated_at: 2026-03-31T11:46:01.239-03:00
generation_type: ai
source_of_truth: codebase
```

> ⚠️ This document was automatically generated by AI via n8n.
>
> Use it as a reference. The source code is the single source of truth.


# AI Agent Guide

## System Overview
The system is a web application that presents an interactive gallery of space photos. It allows users to navigate through different tags, perform global searches, and favorite images for local storage in their browser.

## Architecture
### Overall Structure
- **src/**: Contains the main structure of the React application.
  - `components/`: Reusable modules such as Banner, Card, Footer, Nav, and Populares.
  - `pages/Home`: Main component that renders the complete layout with Header, Footer, and Gallery.

### Main Modules
- **App.jsx**: Entry point for rendering the main application component.
- **Home/index.jsx**: Main page component that displays the gallery of photos.
- **Gallery/index.jsx**: Manages logic to filter and display images based on tags and global searches.
- **Card/index.jsx**: Displays individual image details, including functionalities like favoriting and opening modals.

### Separation of Responsibilities
- **Components** handle UI rendering and interaction logic.
- **Services/localStorage**: Handles local storage for user favorites.
- **Main Entry Point**: Located in `src/main.jsx`.

## Execution Flow
### Entry Points
- The application is initialized and rendered inside the HTML element with id `root` in `src/main.jsx`.
  ```jsx
  import React from 'react';
  import ReactDOM from 'react-dom/client';
  import App from './App.jsx';

  ReactDOM.createRoot(document.getElementById('root')).render(
    <React.StrictMode>
      <App />
    </React.StrictMode>,
  );
  ```

### Main Data Flow
1. **Entry**: The application starts in `src/main.jsx`, which renders the component `App`.
2. **Component App**: The `App` component imports and renders the home page (`Home`).
3. **Page Home**: The home page contains components like Header, Footer, Gallery, and Banner.
4. **Galeria de Fotos**: The gallery is managed by the `Gallery` component, which filters images based on tags or global searches.

### Interactions Between Components
- **Header** interacts with **Home** to receive search values and call the `scrollTo` function.
- **Gallery** receives filtered data through the **Cards** component, which displays the images.
- **Card** allows users to favorite photos and open interactive modals.

## Patterns and Conventions
### File Organization
- Components are located in `src/components`.
- Pages are located in `src/pages`.

### Naming Conventions
- Functions and components follow PascalCase naming convention.
- Directories use lowercase letters.

### Architectural Patterns
- Uses React.js for building the user interface.
- Tailwind CSS and PostCSS for styling and preprocessing CSS.

## DO Rules (Required Practices)
- **Use existing directories**:
  - Place new components in `src/components`.
  - Add new pages to `src/pages`.

- **Preserve naming conventions**:
  - Follow PascalCase for functions and components.
  - Use lowercase letters for directory names.

- **Maintain separation of concerns**:
  - Keep UI logic within components.
  - Store local data using the `services/localStorage` module.

## DON'T Rules (Forbidden Practices)
- Do not create new directories or files outside the established structure without a strong reason.
- Avoid mixing responsibilities across different modules.
- Do not modify existing code unless necessary for feature implementation or bug fixing.

## Anti-Patterns Identified
No clear anti-patterns are identified in the provided codebase. However, avoid creating complex interactions between components that could lead to maintenance issues.

## Modification Rules
### New Features Implementation
- Implement new features by extending existing modules and adding new files as needed.
- Prefer extending over duplicating logic within components or services.

### Extension Points
- Extend functionality within `src/components` for UI-related changes.
- Modify the `services/localStorage` module only if necessary to manage local data storage.

### High-Risk Areas
- **LocalStorage**: Be cautious when modifying functions in `src/services/localStorage`, as they affect user data persistence.
- **Component Interactions**: Avoid complex interactions between components that could lead to maintenance issues.

## Dependencies and Integrations
- **LocalStorage**: Used for storing favorites of users.
  ```js
  export function setLocalStorage(key, value) {
    return window.localStorage.setItem(key, value);
  }

  export function getLocalStorage(key) {
    return window.localStorage.getItem(key);
  }
  ```

## Critical Areas
### Core Logic
- Local storage management in `src/services/localStorage`.
- Interaction logic between components like Header and Home.

### High-Coupling Regions
- Interactions between Gallery, Cards, and Card components.
- Search functionality within the Header component.

## Best Practices for Changes
- **Avoid Duplication**: Extend existing modules instead of duplicating code.
- **Preserve Boundaries**: Maintain separation of concerns by keeping UI logic in components and data management in services.
- **Minimize Regression Risk**: Thoroughly test changes to ensure no unintended side effects occur.
- **Consistency**: Keep new implementations consistent with the current project patterns.

## Feature Implementation Strategy
1. **Start Point**:
   - Begin by understanding the existing structure, particularly `src/pages/Home` and related components.
2. **Inspect Files**:
   - Review `App.jsx`, `Home/index.jsx`, and relevant component files to understand their roles.
3. **Extend Safely**:
   - Add new features within existing modules or create new ones as needed.
4. **Validation**:
   - Test changes locally using `npm run dev` to ensure functionality remains intact.

## Bug Fixing Strategy
1. **Trace Issues**:
   - Use browser console and breakpoints in `src/main.jsx`.
2. **Inspect First**:
   - Start by inspecting the main entry point (`main.jsx`) and related components.
3. **Isolate Faulty Area**:
   - Identify problematic areas through debugging and logging.
4. **Validate Fix**:
   - Thoroughly test changes to ensure no collateral damage.

## Known Unknowns
- No details about infrastructure, databases, or external services used by the application are provided in the codebase.
- The structure does not clearly evidence separation into layers like controllers or services.