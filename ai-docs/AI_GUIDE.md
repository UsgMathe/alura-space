```
generated_by: n8n-workflow
workflow_name: GitHub Repository Context Builder
workflow_id: 8HscJrWZ64ZbBkVr
generated_at: 2026-03-31T14:09:09.440-03:00
generation_type: ai
source_of_truth: codebase
```

> ⚠️ This document was automatically generated by AI via n8n.
>
> Use it as a reference. The source code is the single source of truth.

---

# AI Agent Guide

## System Overview
The system is a web application that displays an interactive gallery of space photos. It allows users to filter photos by tags or titles, add and remove favorites using heart icons, and view detailed images when clicked.

## Architecture
### Overall Structure
- The project uses React components organized in the `src` directory.
- Components are modularized into reusable units within the `components/` folder.
- Services for local storage are located in `services/localStorage`.

### Main Modules
- **App.jsx**: Root component that renders the Home page.
- **pages/Home/index.jsx**: Contains the main logic of the home page, including state management and interactions with child components.
- **components/**: Directory containing reusable UI components such as Banner, Footer, Gallery, etc.

## Execution Flow
### Entry Points
- The entry point is `src/main.jsx`, which initializes the React application by rendering the `App` component.

### Main Data Flow
1. **Initialization**
   - `main.jsx` initializes the application and renders the `App` component.
2. **Rendering Home Component**
   - The `App` component renders the `Home` page.
3. **State Management**
   - The `Home` component manages state for tag filters, search queries, and favorites using React hooks (`useState`, `useEffect`).
4. **Component Interactions**
   - The `Home` component passes properties and functions to child components like `Gallery`.
5. **LocalStorage Integration**
   - Data for favorites is stored in the browser's localStorage.

### Interactions Between Components
- The `Home` component interacts with child components through props and callback functions.
- Child components, such as `Gallery`, manage their own state and call parent-provided functions to update the parent’s state.

## Patterns and Conventions
### File Organization
- **Components**: Reusable UI elements are located in `src/components/`.
- **Pages**: Main pages like Home are found in `src/pages/Home/index.jsx`.

### Naming Conventions
- Files use descriptive names such as `Home.jsx`, `Banner.jsx`, etc.
- Aliases like `@` are used for relative references to the `src/` directory.

### Architectural Patterns
- **React Components**: The application follows standard React component structure with hooks (`useState`, `useEffect`).

## DO Rules (Required Practices)
- Follow existing file and module organization conventions.
- Use React hooks (`useState`, `useEffect`) for state management in components.
- Integrate new features by extending or modifying existing components within the `src/components/` directory.
- Ensure any changes to state management are consistent with how it is handled in `Home.jsx`.
- Preserve the structure and naming conventions observed in the codebase.

## DON'T Rules (Forbidden Practices)
- Do not introduce new architectural layers such as controllers, services, or repositories unless explicitly required by the existing code.
- Avoid duplicating logic that already exists within components.
- Do not modify critical state management functions directly without thorough testing.
- Refrain from altering the `LocalStorage` service outside of its intended use.

## Anti-Patterns Identified
No significant anti-patterns were identified in the provided codebase. However, any new patterns should be carefully evaluated to avoid architectural drift or technical debt.

## Modification Rules
### New Features Implementation
- Implement new features by extending existing components within `src/components/`.
- Prefer reusing and modifying existing logic rather than duplicating it.
- Avoid direct modifications to critical state management functions in `Home.jsx` unless necessary.

### Preservation of Consistency
- Ensure any changes are consistent with the current implementation patterns observed in the codebase.
- Validate new features using build scripts (`npm run dev`, `npm run build`) and linting rules before committing changes.

## Dependencies and Integrations
- **LocalStorage**: The application uses browser's localStorage for storing user favorites data.

## Critical Areas
### Core Logic
- **Home.jsx**: Contains the main logic of state management, including tag filters, search queries, and favorite handling.
- **Gallery/index.jsx**: Manages the display of photos with filtering capabilities based on tags and search queries.

### High-Coupling Regions
- The interaction between `Home` and child components like `Gallery`, `Banner`, etc., is tightly coupled through props and callback functions.

## Best Practices for Changes
- Avoid duplicating existing logic; extend or modify existing components instead.
- Preserve the boundaries of modules and components to maintain consistency with the current implementation.
- Minimize regression risk by thoroughly testing changes using build scripts and linting rules before committing them.

## Feature Implementation Strategy
1. **Start Point**: Begin by extending or modifying existing components within `src/components/`.
2. **Files to Inspect First**: Focus on `Home.jsx` for understanding state management logic.
3. **Safe Extension**: Integrate new features by extending the functionality of existing components rather than duplicating them.
4. **Validation After Change**: Validate changes using build scripts (`npm run dev`, `npm run build`) and linting rules before committing.

## Bug Fixing Strategy
1. **Trace Issues**: Start tracing issues from `Home.jsx` as it contains most of the business logic.
2. **Inspect First**: Inspect the component responsible for the issue first, such as `Gallery`.
3. **Isolate Faulty Area**: Isolate the faulty area by adding logs or console.log statements to trace execution flow.
4. **Validate Fix**: Validate the fix using build scripts and linting rules before committing.

## Known Unknowns
- The infrastructure or database used by the project is not clearly identifiable from the provided code.
- The `photos.json` file containing gallery data is not available in the public repository analyzed.