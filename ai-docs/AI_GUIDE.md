```
generated_by: n8n-workflow
workflow_name: GitHub Repository Context Builder
workflow_id: 8HscJrWZ64ZbBkVr
generated_at: 2026-03-31T13:57:25.464-03:00
generation_type: ai
source_of_truth: codebase
```

> ⚠️ This document was automatically generated by AI via n8n.
>
> Use it as a reference. The source code is the single source of truth.


# AI Agent Guide

## System Overview
The system is a web application that presents an interactive gallery of space photos. It allows users to navigate through different categories (tags) and perform global searches.

## Architecture
### Overall Structure
- **src**: Contains the main structure of the application.
  - **components**: Reusable components like Header, Footer, Card, Gallery, etc.
  - **pages**: Main pages such as Home.
  - **services**: Auxiliary services like localStorage manipulation.
- **public**: Static files.
- **vite.config.js**: Vite configuration for the project.

### Main Modules
- **Components**:
  - `Banner`: Displays an initial banner with a featured image.
  - `Button`: Generic button used in various places.
  - `Card`: Component that displays details and interactions for each photo.
  - `Footer`: Footer containing social links and developer information.
  - `Gallery`: Gallery of photos with functionalities like filtering by tags, global search, and favoriting.
  - `Header`: Header of the application with a search field and navigation button.
  - `Nav`: Navigation sidebar that can be expanded or contracted on mobile devices.

- **Pages**:
  - `Home`: Main page displaying the photo gallery with functionalities like filtering and searching.

- **Services**:
  - `localStorage`: Service for manipulating localStorage, responsible for storing and retrieving user data, mainly favorites.

### Separation of Responsibilities
- **Components**: Each component has a specific responsibility such as displaying information or interacting with the user.
- **Pages**: Pages are composed of several components and provide the main structure of the application.
- **Services**: The `localStorage` service is responsible for local persistence of user data.

## Execution Flow
### Entry Points
The primary entry point is the file `src/main.jsx`, which renders the initial page through the component `App`.

```jsx
import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
```

### Main Data Flow
1. **Initialization**: The `Home` component is rendered through the `App` component.
2. **Search and Filtering**: Global search and filtering by tags are performed in the `Gallery` component, which uses data provided from `photos.json`.
3. **Favoriting**: When a user favorites an image, the local state is updated and saved to localStorage via the service `localStorage`.

### Interactions Between Components
- The `Header` component sends change events to the `onChange` function, which updates the search state in the parent component.
- The photo gallery is rendered based on provided data and filtering/search settings.

## Patterns and Conventions
### File Organization
- **Components**: Reusable components are located in `src/components`.
- **Pages**: Main pages of the application are in `src/pages`.
- **Services**: Auxiliary services are in `src/services`.

### Naming Conventions
- Names of files and folders follow a consistent and descriptive naming convention.

### Architectural Patterns
- The system follows React patterns, including componentization and state management through context or props.
- Tailwind CSS is used for styling.

## DO Rules (Required Practices)
1. **Follow Component Structure**: New features should be implemented within existing components or new components as needed.
2. **Use LocalStorage Service**: For local data persistence, use the `localStorage` service in `src/services/localStorage`.
3. **Maintain Tailwind CSS Styling**: Use Tailwind CSS classes for styling to maintain consistency.
4. **Update JSON Data**: If modifying photo data, ensure changes are reflected in `photos.json`.

## DON'T Rules (Forbidden Practices)
1. **Do Not Modify Main Entry Points Arbitrarily**: Avoid changing the main entry points (`src/main.jsx`, `App.jsx`) unless necessary.
2. **Avoid Direct Manipulation of LocalStorage**: Do not directly manipulate localStorage without using the provided service in `src/services/localStorage`.
3. **Do Not Introduce New External Dependencies Without Approval**.

## Anti-Patterns Identified
- Extensive use of localStorage can create coupling that complicates maintenance and scalability.
- No clear separation into layers like controllers, services, or repositories.

## Modification Rules
1. **New Features**: Implement new features in existing components or add new components as needed within `src/components` or `src/pages`.
2. **Avoid Duplication**: Prefer extending existing code rather than duplicating it.
3. **Preserve Boundaries**: Maintain the current structure and boundaries of modules, files, and services.
4. **Validate Changes**: After making changes, validate them using `npm run dev` to ensure functionality remains intact.

## Dependencies and Integrations
- **LocalStorage**: Used for local storage of user data such as favorites.
- **JSON Data Source**: Photo data is sourced from `photos.json`.

## Critical Areas
1. **Component Gallery**: Central for functionalities like filtering, searching, and favoriting images.
2. **LocalStorage Manipulation**: Sensitive in terms of maintenance and scalability.

## Best Practices for Changes
1. **Avoid Duplication**: Extend existing code rather than duplicating it.
2. **Preserve Boundaries**: Maintain the current structure and boundaries of modules, files, and services.
3. **Minimize Regression Risk**: Ensure changes do not break existing functionality.
4. **Keep Consistent Behavior**: Keep behavior consistent with the current implementation.

## Feature Implementation Strategy
1. **Start Point**: Begin by inspecting `src/components/Gallery/index.jsx` for new feature integration points.
2. **Files to Inspect First**:
   - `src/pages/Home/index.jsx`
   - `src/components/Gallery/Cards/index.jsx`
3. **Extend Safely**: Extend the system safely by adding new components or modifying existing ones as needed.
4. **Validation After Change**: Validate changes using `npm run dev` and ensure all features continue to work correctly.

## Bug Fixing Strategy
1. **Trace Issues**: Use console logs or browser debugging tools in relevant components.
2. **Inspect First**:
   - Check the `Gallery` component for filtering, searching, and favoriting issues.
   - Inspect data sources like `photos.json`.
3. **Isolate Faulty Area**: Isolate faulty areas by analyzing the code flow and console logs.
4. **Validate Fix**: Validate the fix using `npm run dev` to ensure no collateral changes occur.

## Known Unknowns
- **Image Sources**: Details about the sources of images used in the application could not be identified from the provided code.
- **Production Configuration**: The complete configuration for production execution is not evident in the given code.